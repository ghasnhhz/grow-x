import Link from "next/link";
const errors:Record<string,string>={oauth:"X sign-in could not be completed. Please try again.",state:"The sign-in session expired. Please try again.",ineligible:"GrowLeague currently supports public X accounts with 10–5,000 followers.",config:"X sign-in is not configured yet. Please try again later."};
export default async function JoinPage({searchParams}:{searchParams:Promise<{error?:string;plan?:string}>}){const q=await searchParams;return <section className="join-page"><div className="panel join-card"><span className="eyebrow">JOIN GROWLEAGUE</span><h1>Start your 7-day trial.</h1><p>Connect your X account. We place you in the right league and save one follower snapshot per day.</p>{q.error&&errors[q.error]&&<div className="error-box">{errors[q.error]}</div>}{q.plan&&<div className="selected-plan">Selected after trial: <strong>{q.plan}</strong></div>}<div className="join-checks"><span>✓ No ranking boosts for money</span><span>✓ Automatic follower-size league</span><span>✓ One snapshot per day</span></div><Link className="button x-button" href="/api/auth/x/start">𝕏 Continue with X</Link><small>You will not be charged during this auth step. Payments are added in the final Dodo phase.</small></div></section>}
