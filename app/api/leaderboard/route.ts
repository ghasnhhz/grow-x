import { NextRequest } from "next/server"; import { getLeaderboardData } from "@/lib/data"; import type { LeaderboardPeriod, LeaderboardSort, LeagueSlug } from "@/types";
const periods=new Set(["1","7","30"]),sorts=new Set(["gain","growth","followers"]),leagues=new Set(["10-100","101-500","501-1k","1k-5k"]);export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const p=request.nextUrl.searchParams,rawDays=p.get("days")||"7",rawSort=p.get("sort")||"gain",rawLeague=p.get("league");const days=(periods.has(rawDays)?Number(rawDays):7) as LeaderboardPeriod;const sort=(sorts.has(rawSort)?rawSort:"gain") as LeaderboardSort;const league=rawLeague&&leagues.has(rawLeague)?rawLeague as LeagueSlug:undefined;return Response.json({rows:await getLeaderboardData({days,league,sort})});}
