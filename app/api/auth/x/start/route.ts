import { NextResponse } from "next/server"; import { appUrl } from "@/lib/env"; import { createPkce, getXAuthorizationUrl } from "@/lib/x";
export async function GET(){if(!process.env.X_CLIENT_ID)return NextResponse.redirect(`${appUrl()}/join?error=config`);const{verifier,challenge,state}=createPkce();const response=NextResponse.redirect(getXAuthorizationUrl({state,challenge}));const cookie={httpOnly:true,sameSite:"lax" as const,secure:process.env.NODE_ENV==="production",path:"/",maxAge:600};response.cookies.set("gl_x_state",state,cookie);response.cookies.set("gl_x_verifier",verifier,cookie);return response;}
