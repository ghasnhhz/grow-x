import Link from "next/link"; import { LEAGUES } from "@/lib/leagues";
export default function LeaguesPage(){return <section className="page-section"><div className="page-heading"><span className="eyebrow">FAIR COMPETITION</span><h1>Pick your league.</h1><p>Accounts compete against creators with similar follower counts.</p></div><div className="league-grid">{LEAGUES.map(l=><Link className={l.slug==="101-500"?"league-card featured":"league-card"} href={`/?league=${l.slug}#leaderboard`} key={l.slug}><span>{l.label}</span><h2>{l.name}</h2><p>Compete on follower growth, not account size or paid boosts.</p><strong>View league →</strong></Link>)}</div></section>}
