import { and, eq, gte, inArray } from "drizzle-orm";
import { getDb } from "@/db"; import { creators, products, snapshots } from "@/db/schema";
import type { LeaderboardPeriod, LeaderboardRow, LeaderboardSort, LeagueSlug, Plan } from "@/types";
function startDate(days:LeaderboardPeriod){const d=new Date();d.setUTCHours(0,0,0,0);d.setUTCDate(d.getUTCDate()-days);return d;}
export async function getLeaderboardFromDb(o:{days:LeaderboardPeriod;league?:LeagueSlug;sort:LeaderboardSort}):Promise<LeaderboardRow[]>{const db=getDb();const creatorRows=await db.select({id:creators.id,username:creators.username,displayName:creators.displayName,avatarUrl:creators.avatarUrl,followers:creators.followers,league:creators.league,plan:creators.plan,productName:products.name,productUrl:products.url,trialEndsAt:creators.trialEndsAt}).from(creators).leftJoin(products,and(eq(products.creatorId,creators.id),eq(products.isPrimary,true))).where(o.league?and(eq(creators.trackingActive,true),eq(creators.league,o.league)):eq(creators.trackingActive,true));const now=new Date();const activeRows=creatorRows.filter(c=>c.plan!=="trial"||Boolean(c.trialEndsAt&&c.trialEndsAt>=now));if(!activeRows.length)return[];const history=await db.select({creatorId:snapshots.creatorId,followers:snapshots.followers,snapshotDate:snapshots.snapshotDate}).from(snapshots).where(and(inArray(snapshots.creatorId,activeRows.map(r=>r.id)),gte(snapshots.snapshotDate,startDate(o.days))));const map=new Map<string,{followers:number;snapshotDate:Date}[]>();for(const s of history){const list=map.get(s.creatorId)??[];list.push(s);map.set(s.creatorId,list);}const result=activeRows.map(c=>{const h=(map.get(c.id)??[]).sort((a,b)=>a.snapshotDate.getTime()-b.snapshotDate.getTime());const baseline=h[0]?.followers??c.followers;const gain=c.followers-baseline;return{id:c.id,username:c.username,displayName:c.displayName,avatarUrl:c.avatarUrl,currentFollowers:c.followers,gain,growthPercent:Number((baseline>0?gain/baseline*100:0).toFixed(1)),league:c.league as LeagueSlug,plan:c.plan as Plan,productName:c.productName,productUrl:c.productUrl};});return result.sort((a,b)=>o.sort==="growth"?b.growthPercent-a.growthPercent:o.sort==="followers"?b.currentFollowers-a.currentFollowers:b.gain-a.gain);}
