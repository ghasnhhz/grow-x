import Link from "next/link"; import { Avatar } from "@/components/avatar"; import { getLeagueLabel } from "@/lib/leagues"; import type { CreatorProfile } from "@/types";
export function CreatorProfileView({creator}:{creator:CreatorProfile}){const best=creator.history.reduce((a,b)=>b.change>a.change?b:a,creator.history[0]??{date:"—",followers:creator.currentFollowers,change:0});return <div className="profile-page"><section className="profile-hero panel"><div className="profile-identity"><Avatar name={creator.displayName} src={creator.avatarUrl} size={112}/><div><div className="profile-title-line"><h1>{creator.displayName}</h1><span className="league-chip">{getLeagueLabel(creator.league)} League</span></div><p className="handle">@{creator.username}</p><p className="profile-bio">{creator.bio}</p>{creator.location&&<span className="muted">📍 {creator.location}</span>}</div></div><div className="profile-rank"><div><small>Rank this week</small><strong>{creator.rank?`#${creator.rank}`:"—"}</strong></div><div><small>7-day gain</small><strong className="growth-number">+{creator.gain}</strong></div></div><div className="profile-actions"><Link className="button button-primary" href={`/share?username=${creator.username}`}>Share rank card ↗</Link><Link className="button button-gold" href="/pricing">♛ Upgrade</Link><a className="button button-secondary" href={`https://x.com/${creator.username}`} target="_blank" rel="noreferrer">View on X ↗</a></div></section><section className="stats-grid"><Stat label="Current followers" value={creator.currentFollowers.toLocaleString()} detail={`+${creator.gain} this week`}/><Stat label="7-day gain" value={`+${creator.gain}`} detail="Growth buys rank"/><Stat label="Growth %" value={`+${creator.growthPercent.toFixed(1)}%`} detail={`${getLeagueLabel(creator.league)} league`}/><Stat label="Best day" value={`+${best.change}`} detail={best.date}/></section><section className="profile-content-grid"><article className="panel chart-panel"><div className="panel-heading"><h2>7-day growth history</h2><span className="growth-number">+{creator.growthPercent.toFixed(1)}%</span></div><GrowthChart history={creator.history.slice(-7)}/></article><article className="panel snapshots-panel"><div className="panel-heading"><h2>Recent snapshots</h2></div><div className="snapshot-table"><div className="snapshot-row snapshot-head"><span>Date</span><span>Followers</span><span>Change</span></div>{creator.history.slice(-7).reverse().map(p=><div className="snapshot-row" key={p.date}><span>{p.date}</span><span>{p.followers.toLocaleString()}</span><span className="growth-number">{p.change>0?`+${p.change}`:"—"}</span></div>)}</div></article><article className="panel product-panel"><div className="panel-heading"><h2>My product</h2>{["pro","max"].includes(creator.plan)&&<span className="plan-badge pro">pro</span>}</div>{creator.productName&&creator.productUrl?<div className="product-detail"><div className="product-icon">⚡</div><div><strong>{creator.productName}</strong><a href={creator.productUrl} target="_blank" rel="noreferrer">{new URL(creator.productUrl).hostname} ↗</a><p>{creator.productDescription}</p></div></div>:<div className="empty-state">Upgrade to Pro to promote a product here.</div>}</article><article className="panel share-prompt"><h2>Shareable on X</h2><p>Turn your weekly rank into a card your audience can share.</p><Link className="button button-primary" href={`/share?username=${creator.username}`}>Open share card →</Link></article></section></div>}
function Stat({label,value,detail}:{label:string;value:string;detail:string}){return <article className="stat-card"><small>{label}</small><strong>{value}</strong><span>{detail}</span></article>}
function GrowthChart({history}:{history:CreatorProfile["history"]}){if(!history.length)return <div className="empty-state">No snapshots yet.</div>;const values=history.map(x=>x.followers),min=Math.min(...values),max=Math.max(...values),range=Math.max(1,max-min);const points=history.map((p,i)=>`${history.length===1?50:i/(history.length-1)*100},${88-(p.followers-min)/range*68}`).join(" ");return <div className="growth-chart"><svg viewBox="0 0 100 100" preserveAspectRatio="none"><line x1="0" y1="20" x2="100" y2="20"/><line x1="0" y1="42" x2="100" y2="42"/><line x1="0" y1="64" x2="100" y2="64"/><line x1="0" y1="86" x2="100" y2="86"/><polyline points={points}/></svg><div className="chart-labels">{history.map(p=><span key={p.date}>{p.date}</span>)}</div></div>}
